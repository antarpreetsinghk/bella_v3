# Lambda-compatible requirements using FastAPI 0.99.0 + Pydantic V1
# Based on research that this combination works reliably in Lambda

# Core FastAPI with ASGI adapter (known working version)
fastapi==0.99.0
starlette==0.27.0
mangum==0.17.0

# Essential HTTP
httpx==0.28.1
httpcore==1.0.9
python-multipart==0.0.20

# Pydantic V1 (no pydantic-core issues)
pydantic==1.10.18

# Async I/O (pin to working version)
anyio==4.8.0

# Database (minimal footprint)
sqlalchemy==2.0.43
asyncpg==0.30.0
alembic==1.16.4

# Voice & Communication
twilio==9.8.0
phonenumbers==8.13.50

# Time processing
parsedatetime==2.6
python-dateutil==2.8.2

# Configuration
python-dotenv==1.1.1
itsdangerous==2.2.0
typing-extensions==4.12.2

# Cache
redis==5.0.1

# Google Calendar (minimal)
google-api-python-client==2.100.0
google-auth==2.41.1
google-auth-httplib2==0.1.0
# ---- Python ----
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
.eggs/
*.log

# ---- SQLite databases (legacy - no longer used) ----
*.db
*.sqlite
*.sqlite3
data/

# ---- Virtual envs ----
venv/
env/
ENV/
bella_env/

# ---- IDEs ----
.vscode/
.idea/
*.swp
*.swo

# ---- OS files ----
.DS_Store
Thumbs.db

# ---- Build / cache ----
dist/
build/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
.cache/

# ---- Docker ----
# Keep Dockerfile + compose tracked, just ignore build outputs
.wheels/

# ---- Database ----
pgdata/
*.db
*.sqlite3

# ---- Env ----
.env
.env.*
!*.example      # unignore any *.example files (e.g. .env.example)

# ---- Alembic ----
alembic/versions/*.pyc
alembic/versions/__pycache__/
*.env
production*.env

# ---- Security: Internal Documentation ----
# Keep sensitive technical details internal only
docs/internal/
SENSITIVE_DOCS_BACKUP/
TECHNICAL_README.md
ARCHITECTURE.md
DEPLOYMENT.md
DEPLOYMENT_SUMMARY.md
PRODUCTION_GUIDE.md
UNIFIED_DASHBOARD.md
INFRASTRUCTURE.md
OPERATIONS.md
*ARCHITECTURE*
*DEPLOYMENT*
*PRODUCTION*
docs/SECURITY.md
docs/OPERATIONS.md
docs/DEPLOYMENT.md
docs/monitoring-integration-guide.md
docs/cloudwatch-architecture-overview.md
docs/package-architecture.md

# ---- Additional Security Patterns ----
# Never commit production secrets or credentials
*secret*
*password*
*credential*
*private*
*.key
*.pem
*.p12
*.pfx
*.jks
*backup*
*.bak
*.tmp
*dump*
*export*

# Production-specific files
production.*
*-production.*
*production*.env
*prod*.env
aws-*.env
*aws*.env
.env.local*

# Database files and backups
*.sql
*.dump
*.backup

# ---- Sensitive Files ----
# Never commit these types of files
*cookies.txt
*session*
*auth_token*
*api_key*
vulnerability-report.json
security-report.*
audit-*.json

# AWS and Cloud Credentials
aws-credentials.*
.aws/
*ec2*
*rds*
*ecs*
*elb*

# Monitoring and logs that may contain sensitive data
monitoring/*.json
logs/
*.log
*access.log*
*error.log*

# Claude AI settings that may contain credentials
.claude/settings.local.json
.claude/**/settings.local.json

# Terraform
terraform.tfvars
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# ---- CRITICAL: Lambda Environment Files (NEVER COMMIT) ----
# These files contain production API keys and must never be committed
lambda-env.json
lambda-env-update.json
lambda-*.json
*-env.json
*-environment.json

# ---- API Keys and Secrets Patterns ----
# OpenAI API keys
sk-*
sk_*

# Twilio credentials
twilio-*.json
*twilio*.env

# Google Service Account Keys
*service-account*.json
*-key.json
*serviceaccount*.json

# Any file with 'secret', 'key', 'token', 'credential' in name
*secret*.json
*secrets*.json
*key*.json
*keys*.json
*token*.json
*credential*.json
*credentials*.json

# Database connection strings
*database*.json
*db*.json
